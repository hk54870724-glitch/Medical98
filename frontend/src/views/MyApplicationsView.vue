<template><MainLayout><div class="page-card"><h3>我的报销申请</h3><a-table :data="items"><template #columns><a-table-column title="申请单号" data-index="applicationNo"/><a-table-column title="年度" data-index="yearNo"/><a-table-column title="报销日期" data-index="applyDate"/><a-table-column title="发票姓名" data-index="invoiceNames" :width="240"/><a-table-column title="发票号码" data-index="invoiceNos" :width="200"/><a-table-column title="发票金额" data-index="totalInvoiceAmount"/><a-table-column title="报销金额" data-index="totalReimburseAmount"/><a-table-column title="状态"><template #cell="{record}">{{ ['待审批','已通过','已驳回'][record.status] }}</template></a-table-column></template></a-table>
<div v-if="yearTotals.length" style="margin-top:12px;padding:12px 16px;background:#f7f8fa;border-radius:4px;font-size:13px"><div style="font-weight:600;margin-bottom:6px">按年度小计</div><div v-for="t in yearTotals" :key="t.yearNo" style="margin-bottom:4px">{{ t.yearNo }}年度：共 {{ t.count }} 单，小计 ￥{{ t.amount.toFixed(2) }}</div></div>
</div></MainLayout></template>
<script setup>
import {onMounted,ref} from 'vue';import MainLayout from '../layouts/MainLayout.vue';import {getMyApplications} from '../api/reimbursement';const items=ref([]);const yearTotals=ref([]);onMounted(async()=>{const r=await getMyApplications({page:1,pageSize:50});items.value=r.data.items;yearTotals.value=r.data.yearTotals||[];});
</script>
