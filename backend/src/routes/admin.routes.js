import {Router} from 'express';import multer from 'multer';import {requireRoles} from '../middleware/auth.js';import * as c from '../controllers/admin.controller.js';
const router=Router();const upload=multer({storage:multer.memoryStorage(),limits:{fileSize:5*1024*1024}});router.use(requireRoles('ADMIN'));
router.get('/reimbursement-years',c.years);router.post('/reimbursement-years/initialize',c.initializeYear);router.put('/reimbursement-years/:id',c.updateYear);router.delete('/reimbursement-years/:id',c.deleteYear);router.post('/reimbursement-years/:id/activate',c.activateYear);router.post('/reimbursement-years/:id/close',c.closeYear);
router.get('/reimbursement-categories',c.categories);router.post('/reimbursement-categories',c.createCategory);router.put('/reimbursement-categories/:id',c.updateCategory);router.delete('/reimbursement-categories/:id',c.deleteCategory);
router.post('/reimbursement-types',c.createType);router.put('/reimbursement-types/:id',c.updateType);router.delete('/reimbursement-types/:id',c.deleteType);
router.get('/employees',c.employees);router.post('/employees',c.createEmployee);router.put('/employees/:id',c.updateEmployee);router.delete('/employees/:id',c.deleteEmployee);router.post('/employees/:id/enable',c.enableEmployee);router.post('/employees/import',upload.single('file'),c.importEmployees);router.get('/employees/:employeeId/children',c.children);router.post('/employees/:employeeId/children',c.createChild);router.put('/children/:id',c.updateChild);router.delete('/children/:id',c.deleteChild);router.post('/children/:id/enable',c.enableChild);
router.get('/users',c.users);router.post('/users',c.createUser);router.put('/users/:id',c.updateUser);router.post('/users/:id/reset-password',c.resetPassword);router.post('/users/:id/enable',c.enableUser);router.post('/users/:id/disable',c.disableUser);
router.get('/system-parameters',c.parameters);router.put('/system-parameters',c.updateParameters);export default router;
